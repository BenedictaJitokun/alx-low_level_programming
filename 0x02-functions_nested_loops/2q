#include "main.h"
/**
 * _abs - Prints the absolute value of integers
 * Return: Always return 0
 */
int _abs(int)
{
	int r;
	int k = -r;

	if (r >= 0)
	{
		_putchar('r');
	}
	else
	{
		_putchar('k');
	}
}

