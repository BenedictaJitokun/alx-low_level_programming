#include <stdio.h>
/**
 * main - prints a to z
 * Return: Always 0
 */
int main(void)
{
	int ch, CH;

	ch = 'a';

	CH = 'z';

	while (ch <= CH)
	{
	putchar(ch);
	ch++;
	}
	putchar('\n');
	return (0);
}
